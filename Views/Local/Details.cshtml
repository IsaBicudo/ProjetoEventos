@model ProjetoEventos.Models.Local

@{
    ViewData["Title"] = "Detalhes";
}

<h1>Detalhes</h1>

<div>
    <h4>Local</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.LocalNome)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.LocalNome)
        </dd>
    </dl>
    <div class="row">
        <div>
            <img src="@Url.Content(Model.ImagemLocal1)" class="img-geral" />
        </div>
        <div>
            <img src="@Url.Content(Model.ImagemLocal2)" class="img-geral" />
        </div>
        <div>
            <img src="@Url.Content(Model.ImagemLocal3)" class="img-geral" />
        </div>
    </div>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model?.Id" class="btn btn-primary">Editar</a>
    <a asp-action="Index" class="btn btn-primary">Voltar para à lista</a>
</div>
